# Access Control Notice

MkDocs does not enforce access control by default. To keep this section private, restrict access at the hosting layer (e.g., auth gateway, private bucket, or separate deployment target).
